{
  "name": "mirrox",
  "version": "1.1.0",
  "private": true,
  "description": "Mirrox — Android device mirror and control desktop app",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "postinstall": "node scripts/patch-electron-name.mjs",
    "build:packages": "npm run build -w @mirrox/adb && npm run build -w @mirrox/mirror",
    "dev": "npm run build:packages && npm run dev -w @mirrox/desktop",
    "build": "npm run build:packages && npm run build -w @mirrox/desktop",
    "package": "npm run vendor:arm64 && npm run build && npm run package -w @mirrox/desktop",
    "package:dmg": "npm run vendor:arm64 && npm run build && npm run package:dmg -w @mirrox/desktop",
    "package:intel": "npm run vendor:x64 && npm run build && npm run package:intel -w @mirrox/desktop",
    "package:all": "npm run package:dmg && npm run package:intel",
    "vendor": "node scripts/vendor-binaries.mjs",
    "vendor:arm64": "node scripts/vendor-binaries.mjs --arch=arm64",
    "vendor:x64": "node scripts/vendor-binaries.mjs --arch=x64"
  },
  "engines": {
    "node": ">=20"
  },
  "overrides": {
    "brace-expansion": "5.0.9",
    "minimatch": "10.2.6",
    "@electron/asar": "4.2.1"
  }
}
